;; init.el

;; Load startup configuration file
(load (concat user-emacs-directory "core/startup.el"))

;; Load general options configuration file
(load (concat user-emacs-directory "core/options.el"))

;; Load plugin configuration entry point file
(load (concat user-emacs-directory "plugin/init.el"))

;; init.el ends here
